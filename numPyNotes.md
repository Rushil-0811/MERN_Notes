used in data science, 
integrates well with dbs
numpy is written in c

numpy arrays are faster than lists
can perorm vectoised operations
speed and tool to handle large scale operations nicely

import numpy as np
numpy array gives more operations
array = np.array([1,2,3,4])
nd - num dimensional array

multidimensional arrays
ndim - number of dimensions
print(array.ndim)
1 dimensional - single row 
arr = np.arr([[[1,3,3]]])
now the above is a 3d arrayh, iof only two [], would be a 2d array, something like a matrix
need consistent num of eleements within each list, u get homogenous error otherwise
.shape attribute to return a tuple containing the depth, height and width of the 3 d array
multidimensional indexing is fater than chain indexing
word = arr[0,0,0]
imagine u have an 3d array with alphabets

slicing for a 2d array
ARRAY = np. 